import { Injectable } from '@nestjs/common';
import { BaseCrudService } from '../common/crud';
import { {{ pascalName }} } from './model/{{ kebabName }}.model';
import { {{ pascalName }}Repository } from './{{ kebabName }}.repository';

@Injectable()
export class {{ pascalName }}Service extends BaseCrudService<
  {{ pascalName }},
  {{ pascalName }}Repository
> {
  constructor(public readonly {{ camelName }}Repository: {{ pascalName }}Repository) {
    super({{ camelName }}Repository);
  }
}
