import { IsNotEmpty, IsString, Length } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';
import { PaginatedSet } from '../../common/crud';
import { {{ pascalName }} } from '../model/{{ kebabName }}.model';

export class {{ pascalName }}DTO {
  @ApiProperty()
  @IsNotEmpty()
  @IsString()
  @Length(0, 100)
  name: string;
}

export class Paginated{{ pascalName }}Set extends PaginatedSet<{{ pascalName }}[]> {
  @ApiProperty()
  total: number;

  @ApiProperty({ type: [{{ pascalName }}] })
  data: {{ pascalName }}[];
}
